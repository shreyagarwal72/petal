<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- Header line with app/live title and speed chip -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/notification_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="?android:attr/textColorPrimary"
            android:singleLine="true"
            android:ellipsize="middle"
            android:text="Downloading File" />

        <TextView
            android:id="@+id/notification_speed_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@color/md_theme_primary"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:text="0 KB/s • 0%" />
    </LinearLayout>

    <!-- Content line: file size and ETA -->
    <TextView
        android:id="@+id/notification_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="?android:attr/textColorSecondary"
        android:layout_marginTop="2dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="0 MB / 0 MB • calculating..." />

    <!-- Animated doll runner track positioned directly on top of the downloading progress line -->
    <FrameLayout
        android:id="@+id/notification_progress_container"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginTop="2dp">

        <!-- Progress bar directly at bottom of container -->
        <ProgressBar
            android:id="@+id/notification_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_gravity="bottom"
            android:layout_marginBottom="4dp"
            android:max="100"
            android:progress="0" />

        <!-- Doll runner and trophy track overlaying directly upon progress line -->
        <LinearLayout
            android:id="@+id/notification_doll_track"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="bottom">

            <!-- Dynamic spacer positioning runner doll above current progress -->
            <ImageView
                android:id="@+id/notification_doll_runner"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginBottom="2dp"
                android:src="@drawable/ic_doll_run_1"
                android:contentDescription="Live Download Runner" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1"
                android:orientation="horizontal" />

            <!-- Finish Trophy icon positioned at end of downloading line -->
            <TextView
                android:id="@+id/notification_finish_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:gravity="center"
                android:layout_marginBottom="2dp"
                android:textSize="16sp"
                android:text="🏆" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
